name: FL_Bio

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.12.9
  - pip
  
  - pip:
      - flwr[simulation]>=1.13.1
      - numpy>=1.21.0
      - ray>=2.4.0
      - pandas
      - datasets>=3.1.0
      - flwr-datasets>=0.5.0
      - jupyter
      - ipywidgets
      - ipykernel
      - toml
      - matplotlib
      - seaborn
